#include <stdio.h>

/**
 * main - prints the alphabet in lowercase
 *
 * return: Always 0.
 */
int main(void)
{
	char letter;

	for(letter = 'a'a; letter<= 'z'; letter++)
		putchar(letter);

	putchar('\n');

	return (0);
}
